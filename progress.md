# PiPER Dual-Arm Simulation Progress

## 목표

두 대의 AgileX PiPER가 하나의 작업공간에서 충돌을 피하며 협조적으로 움직이고,
최종적으로는 하나의 강체를 양쪽에서 잡아 상대 자세를 유지한 채 운반하는
시뮬레이션 환경을 구축한다.

## 시스템 구조

전체 시스템은 모션 계획과 물리 시뮬레이션을 분리한다.

```text
사용자가 지정한 목표
        ↓
cuRobo: IK, 관절 궤적 최적화, 충돌 회피
        ↓
12관절 공동 궤적
        ↓
MuJoCo: 물리 상태 계산, 접촉 검사, 동시 재생, GUI
```

cuRobo는 목표를 만족하는 관절 경로를 GPU에서 계산하고, MuJoCo는 그 결과가
실제 장면의 로봇과 장애물에 충돌하지 않는지 다시 확인한 뒤 물리 시뮬레이션으로
재생한다. 두 엔진을 분리함으로써 빠른 경로 탐색과 실제 접촉 기반 검증을 함께
사용한다.

## 두 팔의 통합 운동학 모델

두 PiPER를 서로 독립적인 로봇으로 계산하지 않고 하나의 고정 베이스 운동학
트리로 구성했다.

```text
world_base
├── primary PiPER: 6 revolute joints
└── partner PiPER: 6 revolute joints
```

반대편 PiPER는 첫 번째 로봇과 마주보도록 고정 변환을 적용한다. 두 로봇의 관절
이름과 링크 이름에는 각각 고유 prefix를 사용하며, 전체 계획 상태는 12차원
관절 벡터가 된다.

```text
q = [primary_q1 ... primary_q6, partner_q1 ... partner_q6]
```

이를 통해 cuRobo는 한쪽 팔의 결과를 다른 팔의 고정 장애물로 취급하는 대신,
두 팔의 자세와 상호 충돌을 하나의 최적화 문제 안에서 계산할 수 있다.

## 12자유도 공동 IK와 궤적 계획

공유 목표점으로부터 두 팔의 TCP 목표를 계산하고, 두 `gripper_center` 목표를
cuRobo의 multi-link goal로 동시에 전달한다. 공동 IK는 두 TCP 위치를 한 번에
만족하는 12개 관절값을 찾는다. 이후 trajectory optimization도 같은 12자유도
상태공간에서 수행되어 하나의 시간축을 가진 관절 궤적을 생성한다.

```text
shared target
  ├── primary TCP target
  └── partner TCP target
            ↓
joint 12-DOF solve
            ↓
trajectory shape = [time, 12 joints]
```

두 경로를 나중에 합치는 방식이 아니므로 한 팔의 시작을 지연하지 않는다. 각
시점에서 앞의 6개 관절값은 기본 팔에, 뒤의 6개 관절값은 반대편 팔에 동시에
적용한다.

현재 공동 목표는 두 TCP의 위치를 동시에 구속한다. TCP 회전과 두 그리퍼 사이의
고정 상대 변환은 아직 궤적 전체에 걸쳐 구속하지 않는다.

## 그리퍼 표현

모션 계획에서 사용하는 TCP는 두 손가락 사이 중심인 `gripper_center`다. 현재
cuRobo 계획 자유도에는 각 팔의 6개 암 관절만 포함되고 손가락 개폐 관절은
포함되지 않는다.

cuRobo 충돌 계산에서는 손가락의 실제 접촉면을 직접 사용하지 않고,
`gripper_center` 주변의 보수적인 구 형태로 그리퍼 공간을 근사한다. 따라서
현재 모델은 그리퍼가 테이블이나 장애물에 들어가는 경로를 방지할 수 있지만,
손가락 접촉 위치, 파지력, 마찰 및 개방 폭 변화는 계산하지 않는다.

MuJoCo에서는 두 손가락을 별도의 슬라이드 관절과 충돌 형상으로 가지고 있으므로,
실제 파지 단계의 접촉 물리는 MuJoCo가 담당한다.

## 충돌 검증 구조

안전성은 두 단계로 검사한다.

1. cuRobo가 로봇 collision sphere, 자기충돌, 상대 로봇, 테이블 및 장애물을
   반영해 12자유도 경로를 생성한다.
2. 생성된 경로의 waypoint와 waypoint 사이를 MuJoCo에서 다시 보간하고, 실제
   장면 geometry 사이에 접촉이 발생하는지 검사한다.

두 번째 검사는 cuRobo의 단순화된 충돌 형상이 놓칠 수 있는 그리퍼나 메시 수준의
충돌을 재생 전에 차단하는 역할을 한다. 어느 단계에서든 충돌이 확인되면 해당
궤적은 실행하지 않는다.

## 목표 기반 재계획

GUI의 공유 목표는 물리적 물체가 아니라 계획 입력을 나타내는 mocap body다.
목표가 변경되면 실행 중인 궤적을 중단하고 현재 관절 상태에서 새로운 공동
궤적을 계산한다.

목표 위치 변경은 하나의 편집 단위로 기록한다. 이전 위치로 복원하면 로봇의
기존 궤적을 역재생하는 것이 아니라, 복원된 목표와 현재 관절 상태를 사용해
새로운 충돌 회피 궤적을 생성한다. 따라서 환경이나 로봇 상태가 달라져도 이전
경로를 맹목적으로 재사용하지 않는다.

계획 결과는 즉시 제어기에 적용하지 않고 승인 대기 상태로 저장한다. 관절
waypoint마다 MuJoCo 순운동학으로 두 TCP의 월드 위치를 계산해 경로를 먼저
시각화하고, 사용자가 실행을 승인한 뒤에만 같은 관절 궤적을 재생한다. 목표나
환경이 바뀌면 승인 전 궤적을 폐기해 오래된 경로가 실행되지 않도록 한다.

## 공동 계획과 순차 계획 비교

12자유도 공동 계획은 두 로봇을 하나의 운동학 트리로 보고 두 TCP 제약을 같은
최적화 문제에서 해결한다. 순차 계획은 첫 팔의 궤적을 실행하지 않은 채 최종
자세를 예측하고, 이를 두 번째 팔의 장애물로 반영한 다음 두 결과를 함께
재생한다.

두 방식은 로봇 모델의 자유도가 달라 별도 cuRobo 플래너를 유지하고 GUI가 현재
MuJoCo 관절 상태와 목표를 선택된 플래너에 전달한다. 계획 도중 방식을 바꾸면
계획 세대 번호가 지난 응답을 폐기해 두 방식의 결과가 섞이지 않도록 한다.

## 현재까지 검증된 범위

- 두 PiPER를 하나의 12자유도 cuRobo 모델로 로딩
- 두 TCP 위치에 대한 GPU multi-link 공동 IK
- 두 팔의 단일 12자유도 trajectory optimization
- 두 로봇 사이 self-collision 및 고정 장애물 회피
- MuJoCo 장면에서의 사전 접촉 검증
- 동일 시간축을 사용하는 양팔 동시 재생
- 실행 중 목표 변경에 따른 취소 및 재계획

## 강체 공동 운반을 위해 남은 핵심 과제

현재 두 팔은 같은 목표를 기준으로 함께 움직이지만, 하나의 물체를 잡은 폐쇄형
운동학 체인은 아니다. 강체를 비틀림 없이 운반하려면 물체 자세를 시스템의 중심
목표로 사용해야 한다.

물체의 world pose를 `T_WO`, 물체에서 각 grasp frame까지의 고정 변환을
`T_OG1`, `T_OG2`라고 하면 각 시점의 그리퍼 목표는 다음 관계를 만족해야 한다.

```text
T_WG1(t) = T_WO(t) × T_OG1
T_WG2(t) = T_WO(t) × T_OG2
```

이 관계는 최종 위치뿐 아니라 궤적의 모든 시점에서 유지되어야 한다. 다음 단계의
핵심 작업은 다음과 같다.

- 위치와 회전을 포함한 물체의 SE(3) 목표 정의
- 물체 양 끝의 고정 grasp frame 정의
- 모든 waypoint에서 두 TCP의 위치와 회전 동시 구속
- 물체 형상을 cuRobo의 운반 물체 collision geometry로 포함
- MuJoCo에서 물체와 두 그리퍼의 이상적 weld 기반 파지 구현
- 상대 위치·회전 오차와 equality constraint force 감시
- 이후 weld를 실제 손가락 접촉과 마찰 기반 파지로 교체

이 단계가 완료되면 현재의 공동 이동은 단순한 두 TCP 동시 이동에서 실제
폐쇄형 체인 기반의 양팔 강체 운반으로 확장된다.
